<p id="notice"><%= notice %></p>

<h1>Extensions</h1>

<table>
  <thead>
    <tr>
      <th>Permit</th>
      <th>Ext date</th>
      <th>Ext time</th>
      <th>Created by</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @extensions.each do |extension| %>
      <tr>
        <td><%= extension.permit_id %></td>
        <td><%= extension.ext_date %></td>
        <td><%= extension.ext_time %></td>
        <td><%= extension.created_by_id %></td>
        <td><%= link_to 'Show', extension %></td>
        <td><%= link_to 'Edit', edit_extension_path(extension) %></td>
        <td><%= link_to 'Destroy', extension, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Extension', new_extension_path %>

<%= form_with(model: feedback, local: true) do |form| %>
  <% if feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
      <% feedback.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :resource_type %>
    <%= form.text_field :resource_type, id: :feedback_resource_type %>
  </div>

  <div class="field">
    <%= form.label :resource_id %>
    <%= form.number_field :resource_id, id: :feedback_resource_id %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :feedback_comment %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating, id: :feedback_rating %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :feedback_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

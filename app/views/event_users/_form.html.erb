<%= form_with(model: event_user, local: true) do |form| %>
  <% if event_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_user.errors.count, "error") %> prohibited this event_user from being saved:</h2>

      <ul>
      <% event_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id, id: :event_user_event_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :event_user_user_id %>
  </div>

  <div class="field">
    <%= form.label :rsvp %>
    <%= form.text_field :rsvp, id: :event_user_rsvp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

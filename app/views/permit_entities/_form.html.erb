<%= form_with(model: permit_entity, local: true) do |form| %>
  <% if permit_entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(permit_entity.errors.count, "error") %> prohibited this permit_entity from being saved:</h2>

      <ul>
      <% permit_entity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :permit_entity_name %>
  </div>

  <div class="field">
    <%= form.label :permit_id %>
    <%= form.number_field :permit_id, id: :permit_entity_permit_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :permit_entity_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

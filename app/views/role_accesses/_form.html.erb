<%= form_with(model: role_access, local: true) do |form| %>
  <% if role_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role_access.errors.count, "error") %> prohibited this role_access from being saved:</h2>

      <ul>
      <% role_access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.text_field :site_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

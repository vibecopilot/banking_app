<%= form_with(model: sub_group, local: true) do |form| %>
  <% if sub_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_group.errors.count, "error") %> prohibited this sub_group from being saved:</h2>

      <ul>
      <% sub_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id, id: :sub_group_group_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :sub_group_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

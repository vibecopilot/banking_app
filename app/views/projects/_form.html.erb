<div class="modal fade" id="addp<%= @project.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" style="display: none;">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title" id="myModalLabel">Add</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
      </div>
      <div class="modal-body">
        <%= form_for(@project) do |form| %>
        <div class="row">
          <label>Name</label>
          <%= form.text_field :name, id: :project_name %>
        </div>
        <% end %>
     </div>
     <div class="modal-footer">
       <button type="submit" class="btn btn-info addprj">Add</button>
     </div>
   </div>
 </div>
</div>

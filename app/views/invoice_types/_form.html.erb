<%= form_with(model: invoice_type, local: true) do |form| %>
  <% if invoice_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice_type.errors.count, "error") %> prohibited this invoice_type from being saved:</h2>

      <ul>
      <% invoice_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :invoice_type_name %>
  </div>

  <div class="field">
    <%= form.label :created_by_id %>
    <%= form.number_field :created_by_id, id: :invoice_type_created_by_id %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.number_field :site_id, id: :invoice_type_site_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: user_category, local: true) do |form| %>
  <% if user_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_category.errors.count, "error") %> prohibited this user_category from being saved:</h2>

      <ul>
      <% user_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

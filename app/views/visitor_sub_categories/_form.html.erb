<%= form_with(model: visitor_sub_category, local: true) do |form| %>
  <% if visitor_sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visitor_sub_category.errors.count, "error") %> prohibited this visitor_sub_category from being saved:</h2>

      <ul>
      <% visitor_sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :visitor_category_id %>
    <%= form.text_field :visitor_category_id, id: :visitor_sub_category_visitor_category_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :visitor_sub_category_name %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :visitor_sub_category_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
